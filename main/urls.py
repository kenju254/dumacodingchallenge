from django.conf.urls import patterns, include, url

urlpatterns = patterns('',
   #url('^$', 'userslocations.views.select_user'),
    #url('^location/$', 'userslocations.views.select_location'),
    #url('^success/$', 'userslocations.views.show_assignment'),
)